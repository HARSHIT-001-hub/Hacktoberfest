for (let i = 0; ; i++) {
    console.log("The Number is: ",i);
}

// This loop will run indefinitely because there is no terminating condition.
// To terminate the loop, Press ctrl + C in the terminal where the script is running.